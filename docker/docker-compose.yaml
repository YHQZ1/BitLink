version: "3.9"

services:

  nginx:
    image: nginx:alpine
    container_name: bitlink-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /etc/nginx/.htpasswd:/etc/nginx/.htpasswd:ro
    depends_on:
      backend:
        condition: service_started
    restart: unless-stopped
    networks:
      - bitlink-network


  backend:
    image: ghcr.io/yhqz1/bitlink-backend:prod
    container_name: bitlink-backend
    env_file:
      - ./backend.env
    expose:
      - "3000"
      - "9100"
    restart: unless-stopped
    networks:
      - bitlink-network


  queue-worker:
    image: ghcr.io/yhqz1/bitlink-backend:prod
    container_name: bitlink-worker
    env_file:
      - ./backend.env
    command: ["node", "workers/analytics.worker.js"]
    expose:
      - "9101"
    restart: unless-stopped
    networks:
      - bitlink-network


  prometheus:
    image: prom/prometheus:latest
    container_name: bitlink-prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus-data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
    ports:
      - "9090:9090"
    restart: unless-stopped
    networks:
      - bitlink-network


  grafana:
    image: grafana/grafana:latest
    container_name: bitlink-grafana
    ports:
      - "3001:3000"
    volumes:
      - grafana-storage:/var/lib/grafana
    restart: unless-stopped
    networks:
      - bitlink-network


networks:
  bitlink-network:
    driver: bridge


volumes:
  grafana-storage:
  prometheus-data: